# Create a virtual enviroment of Pythonv3+
python3 -m venv env

# Activate the env
source env/bin/activate # For Unix systems
venv/Scripts/activate # For Windows

# Then install the package
pip install -e .

# use python
python

# import the package
from pylexicalanalyzer import *

# after using deactivate the virtual enviroment
deactivate